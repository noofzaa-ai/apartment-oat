# คู่มือผู้ใช้ — ระบบจัดการหอพัก

เข้าถึงที่: `https://apartments.daiyooo.com`

---

## เริ่มต้นใช้งาน (Get Started)

### สำหรับผู้ใช้ใหม่
1. Login ครั้งแรก → ระบบพาไปหน้า `/get-started` อัตโนมัติ
2. เลือก pricing plan:
   - **TRIAL** (แนะนำ): ฟรี 30 วัน, จำกัด 10 ห้อง
   - **STARTER**: 5฿/ห้อง/เดือน
   - **STANDARD**: 8฿/ห้อง/เดือน (รองรับ Room Preset)
   - **PRO**: 12฿/ห้อง/เดือน (ฟีเจอร์ครบ)
3. คลิก **"เริ่มทดลองฟรี 30 วัน"** → เริ่มใช้งานได้ทันที
4. Paid plans: ติดต่อทีมงาน (Payment gateway Phase 6)

### ฟีเจอร์บนหน้า Get Started
- **กล่องแพลน 4 อัน**: แสดงฟีเจอร์แต่ละแพลนครบถ้วน
- **ปุ่ม "🎟️ ใช้รหัสเชิญ"**: 
  - เปิด modal กรอกรหัสเชิญจากเจ้าของหอ
  - รองรับทั้งรหัสสั้น (8 ตัว: `zn3quM-C`) และ URL เต็ม
  - Paste ได้ทั้ง 2 รูปแบบ ระบบ parse อัตโนมัติ
- **ปุ่ม "🚪 ออกจากระบบ"**: มุมบนขวา สำหรับเปลี่ยน account

---

## การเข้าสู่ระบบ

### สำหรับทุกคน (เจ้าของและผู้เช่า)
1. ไปที่หน้า `/login` หรือคลิกปุ่ม "เข้าสู่ระบบ" จากหน้าแรก
2. ระบบจะนำคุณไปยัง **Daiyooo Account** (SSO) เพื่อยืนยันตัวตน
3. login ด้วยบัญชี Daiyooo ของคุณ
4. หลัง login สำเร็จ:
   - **ถ้าคุณมีหอพักอยู่แล้ว** (เจ้าของ) → ไปที่ `/app/locations` (รายการหอ)
   - **ถ้าคุณยังไม่มีหอ** → ไปที่ `/tenant/dashboard` (หน้าผู้เช่า)

### ข้อจำกัดปัจจุบัน
- ระบบยังไม่รองรับการเลือกบัญชี email หลายตัวในหน้า login (account chooser) เนื่องจาก Daiyooo Account ยังไม่รองรับ safe account-switch flow
- หาก login ด้วยบัญชี email อื่นอยู่ ระบบจะใช้บัญชีนั้นโดยอัตโนมัติ

---

## เจ้าของหอพัก (Owner)

### เริ่มต้นใช้งาน
1. หลัง login ครั้งแรก ถ้ายังไม่มี subscription:
   - คลิก **"เริ่มทดลองใช้งานฟรี 30 วัน"**
   - ระบบสร้าง trial subscription ให้อัตโนมัติ
2. เมื่อมี subscription แล้ว สามารถ **สร้างหอพัก** ได้

### จัดการหอพัก (`/app/locations`)
- แสดงรายการหอพักทั้งหมดที่คุณเป็นเจ้าของ
- **เพิ่มหอใหม่**: คลิกปุ่ม "เพิ่มหอพัก" แล้วกรอก ชื่อ/ที่อยู่/เบอร์โทร
- **จัดการห้อง**: คลิก "ดูห้อง →" เพื่อไปจัดการห้องภายในหอนั้น
- แก้ไขหรือลบหอ: ใช้เมนู action ในแต่ละการ์ด

### จัดการห้อง (`/app/locations/[id]/rooms`)
- เพิ่ม/แก้/ลบห้อง พร้อมตั้งค่า:
  - ประเภทห้อง
  - ค่าเช่าพื้นฐาน
  - อัตราค่าน้ำ/ค่าไฟต่อหน่วย
  - บริการเสริม (ที่จอดรถ/Wi-Fi/แอร์) + ราคาแต่ละรายการ

### อ่านมิเตอร์ (`/app/locations/[id]/meter-reading`)
1. เลือกเดือนที่ต้องการบันทึก
2. กรอกเลขมิเตอร์ปัจจุบัน (น้ำ/ไฟ) ในแต่ละห้อง
3. ระบบแสดงเลขมิเตอร์เดือนก่อนให้เทียบ
4. บันทึก → ระบบคำนวณหน่วยที่ใช้และสร้างบิลอัตโนมัติ

### จัดการบิล (`/app/locations/[id]/bills`)
- ดูบิลทั้งหมดตามเดือน/ห้อง
- สถานะ: **ยังไม่ชำระ** / **แจ้งชำระแล้ว** / **ชำระแล้ว**
- ตรวจสลิปจากผู้เช่า: อนุมัติ (→ PAID) หรือปฏิเสธ (→ UNPAID + ระบุเหตุผล)
- ดาวน์โหลด PDF บิล

### เชิญผู้เช่า (`/app/invites`)
1. สร้าง invite code สำหรับห้องที่ต้องการ
2. ระบบสร้างรหัส 8 ตัว (base64url) เช่น `zn3quM-C`
3. ส่ง **URL เต็ม** `https://apartments.daiyooo.com/tenant/invite/[code]` หรือ **รหัสสั้น** ให้ผู้เช่า
4. ผู้เช่าสามารถ:
   - คลิก URL เต็ม → ไปหน้า claim
   - Paste ลงใน modal บนหน้า `/get-started` (รองรับทั้ง 2 รูปแบบ)
5. หลัง claim สำเร็จ → ผู้เช่าเข้าถึงห้องได้

### สลับไปโหมดผู้เช่า
- คลิกลิงก์ "โหมดผู้เช่า" ในเมนู → ไปที่ `/tenant/dashboard`

---

## ผู้เช่า (Tenant)

### เข้าสู่ระบบ
1. ไปที่ `/login` → login ด้วย Daiyooo Account
2. หลัง login:
   - **ถ้ายังไม่มีห้อง** → เห็นหน้า dashboard พร้อม CTA "สร้างหอ / ใช้งานโหมดเจ้าของ"
   - **ถ้ามีห้องแล้ว** → เห็นข้อมูลห้อง/บิล

### Claim ห้อง (2 วิธี)

**วิธีที่ 1: ผ่าน URL เต็ม**
1. เจ้าของหอส่ง URL `https://apartments.daiyooo.com/tenant/invite/[code]`
2. คลิก URL → login (ถ้ายังไม่ได้ login)
3. กดปุ่ม "ยืนยันเข้าพัก"
4. ระบบผูกห้องกับบัญชี

**วิธีที่ 2: ผ่าน Modal บนหน้า Get Started**
1. Login แล้วไปที่ `/get-started`
2. คลิกปุ่ม "🎟️ ใช้รหัสเชิญ"
3. Paste รหัส (8 ตัว: `zn3quM-C`) หรือ URL เต็ม
4. กด Submit → ระบบ claim อัตโนมัติ

### ดูบิล (`/tenant/dashboard`)
- แสดงบิลทั้งหมดของห้องที่คุณเช่า
- ข้อมูล: เดือน, จำนวนเงิน, สถานะ, รายละเอียด (ค่าน้ำ/ไฟ/บริการเสริม)

### แจ้งชำระเงิน
1. เลือกบิลที่ต้องการชำระ
2. อัปโหลดสลิปโอนเงิน
3. สถานะเปลี่ยนเป็น **"แจ้งชำระแล้ว"**
4. รอเจ้าของตรวจสอบและอนุมัติ

### สลับไปโหมดเจ้าของ
- คลิกลิงก์ "โหมดเจ้าของ" (ถ้าคุณมี subscription) → ไปที่ `/app/locations`

---

## ข้อมูลผู้ใช้

### การแสดงผล
- ชื่อ (displayName) และ email ดึงจาก **Daiyooo Account** (userinfo)
- แสดงใน UI ทั้งโหมดเจ้าของและผู้เช่า

### Logout
1. คลิก "ออกจากระบบ"
2. ระบบล้าง session ภายในแอป
3. ส่งต่อไปยัง Daiyooo Account logout (ถ้า provider รองรับ)
4. login ครั้งถัดไป: ต้องยืนยันตัวตนใหม่ผ่าน Daiyooo SSO

---

## สิทธิ์และข้อจำกัด

### Subscription
- **สร้างหอพัก**: ต้องมี subscription (TRIAL หรือ ACTIVE)
- **Trial**: ฟรี 30 วัน หลังจากนั้นหมดอายุ (EXPIRED)
- หมดอายุ: สร้างหอ/ห้องใหม่ไม่ได้ (อ่านของเดิมยังได้)
- ผู้เช่าในหอยังใช้งานได้ตามปกติ

### การเข้าถึงข้อมูล
- **เจ้าของ**: จัดการได้เฉพาะหอที่ตัวเองสร้าง (scoped by `ownerUserId`)
- **ผู้เช่า**: เห็นเฉพาะข้อมูลห้องที่ตัวเองผูกอยู่ (scoped by `membership.roomId`)
- สิทธิ์ไม่ข้าม apartment: เป็น owner หอ A ไม่สามารถจัดการหอ B ได้

### บทบาทซ้อน
- คนเดียวกันสามารถเป็นทั้ง **owner** (หอตัวเอง) และ **tenant** (ไปเช่าหอคนอื่น) ได้พร้อมกัน
- สลับโหมดผ่านลิงก์ใน UI

---

## ปัญหาที่พบบ่อย

**Q: ทำไมกด login แล้วไม่สามารถเลือกบัญชี email อื่นได้?**  
A: Daiyooo Account (Better Auth) ปฏิเสธพารามิเตอร์ `prompt=select_account` ระบบจึงไม่สามารถบังคับเปิด account chooser ได้ในขณะนี้ ต้องรอ Daiyooo Account รองรับ safe account-switch flow

**Q: ผู้เช่าที่ยังไม่มีห้องจะทำอะไรได้บ้าง?**  
A: เห็นหน้า dashboard พร้อม CTA "สร้างหอ / ใช้งานโหมดเจ้าของ" สามารถเริ่มทดลองใช้ subscription และสร้างหอเป็นเจ้าของได้

**Q: เจ้าของหอหลายแห่งจัดการอย่างไร?**  
A: หน้า `/app/locations` แสดงหอทั้งหมดที่คุณเป็นเจ้าของ เลือกหอที่ต้องการจัดการ

**Q: ตัด subscription ของเจ้าของแล้วผู้เช่าได้รับผลกระทบไหม?**  
A: ในเฟสปัจจุบัน ผู้เช่ายังเข้าถึงบิลและแจ้งชำระได้ตามปกติ (hard-freeze ยังไม่มี)

---

## Technical Notes

- **Platform**: Next.js 16 App Router + React 19 + Prisma/SQLite
- **SSO Provider**: Daiyooo Account (Better Auth) ที่ `account.daiyooo.com`
- **Session**: iron-session cookie (`apt_session`), server-side only
- **Deploy**: Docker container `apartment-oat-app` (port 3004→3000) หลัง Cloudflare
- **Production URL**: `https://apartments.daiyooo.com`
